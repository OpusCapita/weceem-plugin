h4. Purpose

The space tag outputs the name of the current space. It takes only the optional "codec" attribute to perform escaping to JavaScript or HTML and so on. Defaults to HTML escaping.

*Example*

{code}
Current space: <wcm:space/>
{code}

